# Disable Next.js auto-patching of SWC dependencies
# This prevents "Failed to patch lockfile" errors in monorepo setups
SKIP_SWC_OPTIMIZATION=1

# Disable workspace detection
legacy-peer-deps=true
workspaces=false
